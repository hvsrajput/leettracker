{
  "name": "leettracker-root",
  "version": "1.0.0",
  "scripts": {
    "start": "npm start --prefix server & npm run dev --prefix client",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server"
  }
}
